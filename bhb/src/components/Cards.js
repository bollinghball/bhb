import React, { Component } from 'react';
import styled from 'styled-components';

const Cards = styled.section`
    /* display: flex;
    justify-content: center; */
`;

export default class extends React.Component {
    render() {
        return (
            <Cards className="portfolio">

            </Cards>
        );
    }
}